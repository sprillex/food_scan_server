Act as a Data Entry Specialist. Your goal is to digitize the Nutrition Facts label and Barcode from this image.

INSTRUCTIONS:
1. SCAN BARCODE: Look for the UPC/EAN barcode (12 or 13 digits). If visible, extract it.
2. OCR LABEL: Extract the standard nutrition values per serving.
3. OUTPUT FORMAT: Return ONLY valid JSON. Do not include markdown formatting or chat text.

Use this exact JSON structure:

{
  "variables": {
    "{{ITEM_NAME}}": {
      "name": "{{ITEM_NAME}}",
      "brand_name": "{{BRAND}}",
      "upc": "{{UPC_DIGITS}}",
      "calories": {{CALORIES_INT}},
      "fat": {{FAT_GRAMS}},
      "carbohydrates": {{CARBS_GRAMS}},
      "protein": {{PROTEIN_GRAMS}},
      "serving_size": "{{SERVING_TEXT}}"
    }
  }
}

If a value is not visible, use null.
